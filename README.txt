KB PatchOnly — LTC/SNF Clinical Standards & Triage (Regulatory + Best Practice)

Adds:
- public/kb/docs/standards/ltc_snf_clinical_standards_and_triage.json

Apply:
1) Unzip into your repo root (same folder as package.json), preserving paths.
2) Rebuild to regenerate search_index.json:
   cmd /c "npm run build"

Result:
- This document becomes searchable and can be added to Packet Draft via the Reader modal.
